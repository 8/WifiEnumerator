<UserControl x:Class="WifiEnumerator.View.RefreshView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             >
  <Grid>
    
    <StackPanel Orientation="Horizontal" >
      <CheckBox Content="Refresh Automatically" IsChecked="{Binding IsAutoRefreshEnabled}" VerticalAlignment="Center"/>
      <Label Content="Interval:" VerticalAlignment="Center"/>
      <TextBox Text="{Binding RefreshInterval}" MinWidth="40" VerticalAlignment="Center" />
      <Label Content="ms" VerticalAlignment="Center"/>
    </StackPanel>    

  </Grid>
</UserControl>
